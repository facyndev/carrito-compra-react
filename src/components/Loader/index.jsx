import { LoaderContainer, LoaderItem } from "./loader.style"

export const Loader = () => {
    return (
        <LoaderContainer title="Cargando..." >
            <LoaderItem />
        </LoaderContainer>
    )
}

